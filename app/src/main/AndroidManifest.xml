<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.m6code.cryptocurrencyconverter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".BtcActivity"
            android:label="Crypto Converter">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EthActivity"
            android:label="Ethereum Rate"
            android:parentActivityName=".BtcActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.m6code.cryptocurrencyconverter.BtcActivity" />
        </activity>
        <activity
            android:name=".ConvertActivity"
            android:label="Convert"
            android:parentActivityName=".BtcActivity" />
    </application>

</manifest>